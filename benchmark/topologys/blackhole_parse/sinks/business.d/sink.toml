version = "2.0"

[sink_group]
name = "all"
#parallel = 2
parallel = 4
rule= ["*"]
#tags = ["group:benchmark", "type:business"]

# Null sink 用于性能基准测试
[[sink_group.sinks]]
name = "blackhole"
connect = "blackhole_sink"
#tags = ["sink:null", "purpose:benchmark"]

# 可选：启用文件输出进行调试
# [[sink_group.sinks]]
# name = "debug_output"
# connect = "file_json_sink"
# params = { base = "./out", file = "benchmark_debug.json" }
