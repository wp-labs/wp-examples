name : /oml/example/simple

rule :
    /example/simple*
---
recv_time  = take() ;
occur_time = Now::time() ;
from_ip    = take(option:[from-ip]) ;
src_ip     = take(option:[src-ip,sip,source-ip] );
*  = take() ;
