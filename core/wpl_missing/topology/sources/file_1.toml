key = "file_1"                 # 源标识
enable = true                   # 启用
connect = "file_src"     # 引用 connectors/source.d 中的连接器 id
# 如需覆盖连接器默认参数，可在此处指定；默认路径指向 data/in_dat
[params]
base = "./data/in_dat"
file = "gen.dat"
encode = "text"
