version = "2.0"

[sink_group]
name = "miss"
oml = []
batch_size = 10
[[sink_group.sinks]]
connect = "file_raw_sink"
params = {  file = "miss.dat" , sync = "true"  }
[sink_group.sinks.expect]
# 该组为异常统计，采用范围期望而非比例
ratio = 6.00
tol   = 0.01
